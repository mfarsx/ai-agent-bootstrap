Review all files in `docs/context/` and update them to reflect the current project state:

1. Read every file in `docs/context/`
2. Compare with actual codebase state
3. Update `activeContext.md` with current focus, recent changes, next steps
4. Update `progress.md` with completed work and remaining tasks
5. Update other files only if they contain outdated information
6. Append new info — don't delete existing content unless it's wrong
7. Report what you changed
