---
description: "Hard constraints — never break without explicit user approval"
alwaysApply: true
---

# Boundaries

These rules are non-negotiable. Do NOT break them without explicit user approval.

## Never
- Delete or rename files without asking
- Install major dependencies (framework, ORM, CSS lib) without discussion
- Change the build system or toolchain
- Modify `.env` files or commit secrets/API keys
- Push to git — user handles all git operations
- Modify files outside the project root

## Always
- Match existing code style and patterns
- Check if a file exists before creating one with a similar name
- Prefer existing dependencies over adding new ones
- Consider bundle size — small, focused packages over kitchen-sink libraries
- Verify `public/` asset references exist after changes
